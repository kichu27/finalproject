
export const metadata = {
  title: 'Verify Email',
  description: 'Hold on while',
};

export default function RootLayout({ children }) {
 
    return (
       <html lang="en">
        
         <body>{children}</body>
       </html>
     )
   }
   